plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':lib')
    implementation project(':api')
    runtimeOnly project(':pluginA')
    runtimeOnly project(':pluginB')
}

application {
    mainClass = 'edu.curtin.terminalgriddemo.TerminalGridDemo'
}

run{
 standardInput = System.in
}
